# Development CORS origins (Vite + React frontend)
CORS_ORIGINS=http://localhost:5173
# For production, set this to your deployed frontend domain (e.g., https://resume-scanner.jakubstetz.dev)

# Hugging Face Transformers models
NER_MODEL=Jean-Baptiste/roberta-large-ner-english
SIMILARITY_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Alternate lightweight AI NER model for development
# Use these to reduce loading time and RAM usage
# This is the model used in the live demo to avoid unnecessary hosting costs
# NER_MODEL=dslim/bert-base-NER